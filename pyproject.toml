[project]
name = "designer-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi-cli>=0.0.20",
    "fastapi[standard]>=0.128.0",
    "python-dotenv>=1.2.1",
    "vercel-sandbox>=0.0.5",
]
